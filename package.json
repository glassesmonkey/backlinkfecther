{
  "name": "backliner-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal executable backbone for shared-CDP backlink submission workflows.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start-browser": "pnpm build && node dist/cli/index.js start-browser",
    "preflight": "pnpm build && node dist/cli/index.js preflight",
    "enqueue-site": "pnpm build && node dist/cli/index.js enqueue-site",
    "claim-next-task": "pnpm build && node dist/cli/index.js claim-next-task",
    "task-prepare": "pnpm build && node dist/cli/index.js task-prepare",
    "task-record-agent-trace": "pnpm build && node dist/cli/index.js task-record-agent-trace",
    "task-finalize": "pnpm build && node dist/cli/index.js task-finalize",
    "run-next": "pnpm build && node dist/cli/index.js run-next"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "typescript": "^5.8.3"
  }
}
